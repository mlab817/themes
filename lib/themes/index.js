'use strict';

var colors = require('./colors');
var spacing = require('./spacing');
var sizes = require('./sizes');
var fonts = require('./fonts');

module.exports = {
  colors: colors,
  spacing: spacing,
  sizes: sizes,
  fonts: fonts
};